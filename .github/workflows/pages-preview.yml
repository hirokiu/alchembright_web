name: Deploy custom-domain Pages
on:
  push:
    branches: [docs/wordpress-migration-audit]
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: github-pages
  cancel-in-progress: false
jobs:
  build:
    if: github.ref == 'refs/heads/docs/wordpress-migration-audit' && vars.PAGES_PREVIEW_ENABLED == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: read
    env:
      ASTRO_TELEMETRY_DISABLED: '1'
      PUBLIC_SITE_MODE: production
      ALCHEMBRIGHT_SITE: https://www.alchembright.com
      ALCHEMBRIGHT_BASE: /
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version-file: .nvmrc
          cache: npm
      - name: Require intended Pages configuration
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api "repos/$GITHUB_REPOSITORY/pages" --jq '{cname,build_type}' > /tmp/pages.json
          node --input-type=module -e 'import fs from "node:fs"; const p=JSON.parse(fs.readFileSync("/tmp/pages.json")); if(p.cname!=="www.alchembright.com" || p.build_type!=="workflow") throw Error("Expected www.alchembright.com and GitHub Actions source");'
      - uses: actions/configure-pages@983d7736d9b0ae728b81ab479565c72886d7745b # v5
      - run: npm ci
      - run: npm test
      - run: npm run check
      - name: Regenerate articles, category/tag/date archives and thumbnails
        run: npm run build
      - name: Verify root robots.txt permits crawling
        run: |
          node --input-type=module -e 'import fs from "node:fs"; if(fs.readFileSync("dist/robots.txt","utf8").trim()!=="User-agent: *\nAllow: /\nSitemap: https://www.alchembright.com/sitemap.xml") throw Error("Unexpected robots policy");'
      - name: Verify published site
        run: npm run migration:verify -- --release
      - uses: actions/upload-pages-artifact@56afc609e74202658d3ffba0e8f6dda462b719fa # v3
        with:
          path: dist
  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4
