# MTバックアップからのタグ追加（2026-09-09）

タイトル・日時が完全一致414件。さらに日時・basenameが一致し、タイトルが引用符・三点リーダーの置換だけで異なる3件を同一記事と確認。合計417件（バックアップ公開415件、当時下書きだが現WPで公開済み2件）。公開記事で新規追加対象は0件。バックアップのみにあるのは下書き12件で、原本を非公開で保持し、匿名化案を本人確認した後に公開する。現時点では全件未承認で、公開対象から除外。

45記事に70件のタグ関連付けを追加。既存タグの削除・カテゴリ変更は行わない。422件の本文をバイト単位で比較し、タグ配列以外のメタデータ（タイトル・日時・URLを含む）もすべて一致した。

migration/mappings/mt-tag-additions.json に確認済みの追加入力を保持する。元ファイルの個人情報・下書き本文は公開しない。import.mjs が原本検証後にタグを統合するため、再取り込みでも補完情報を保持する。新規タグはWordPress採番と衝突しない負数IDを割り当てる。名前一致の既存タグを優先し、再実行で重複しない。元WPレコードのハッシュは変更前のものを保持する。

新スナップショットで対象記事のタイトル・日時が変わった場合は停止して再照合する。元記事の削除が起きた場合も自動継続しない。
